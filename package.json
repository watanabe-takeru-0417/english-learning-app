{
  "name": "english-learning-app",
  "private": true,
  "workspaces": [
    "mcp-servers/*",
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev:frontend": "npm run dev --workspace=frontend",
    "dev:backend": "npm run dev --workspace=backend",
    "build": "npm run build --workspaces",
    "mcp:gemini": "npm run start --workspace=gemini-server",
    "mcp:grok": "npm run start --workspace=grok-server",
    "mcp:codex": "npm run start --workspace=codex-server"
  }
}
